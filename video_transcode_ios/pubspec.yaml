name: video_transcode_ios
description: iOS implementation of the video_transcode plugin
version: 0.1.0+1


environment:
  sdk: ">=3.0.0 <4.0.0"

flutter:
  plugin:
    implements: video_transcode
    platforms:
      ios:
        pluginClass: VideoTranscodePlugin

dependencies:
  flutter:
    sdk: flutter
  video_transcode_platform_interface:
    path: ../video_transcode_platform_interface

dev_dependencies:
  flutter_test:
    sdk: flutter
  plugin_platform_interface: ^2.0.0
  very_good_analysis: ^5.0.0
